{
  "name": "kode-desktop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:editor": "node scripts/build",
    "build:neu": "neu build",
    "watch:editor": "node scripts/watch",
    "watch:neu": "neu run",
    "desktop": "npm-run-all -p watch:editor watch:neu",
    "build": "npm-run-all -p build:editor build:neu"
  },
  "dependencies": {
    "@neutralinojs/neu": "^9.2.0",
    "editor": "*",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
