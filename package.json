{
  "name": "kode-shared",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "version": "0.0.0",
  "scripts": {
    "prepare": "npm run prepare --workspace=kode-desktop",
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "desktop": "turbo run desktop"
  },
  "devDependencies": {
    "@types/node": "^17.0.18",
    "esbuild": "^0.14.23",
    "turbo": "^1.1.3",
    "typescript": "^4.5.5"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "npm@8.1.0"
}
