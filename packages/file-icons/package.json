{
  "name": "icons",
  "version": "0.0.0",
  "private": true,
  "main": "./map.js",
  "scripts": {
    "prebuild": "rm -rf dist && mkdir -p dist",
    "build": "npx fantasticon"
  },
  "files": [
    "dist",
    "map.js",
    "prefix.js"
  ]
}
