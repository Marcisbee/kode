{
  "name": "editor",
  "version": "0.0.0",
  "main": "./src/kode.ts",
  "types": "./src/kode.ts",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild --bundle --format=esm --legal-comments=external src/main.ts --sourcemap --servedir=public",
    "build": "esbuild --bundle --format=esm --legal-comments=external src/kode.ts --minify --drop:console --outdir=dist --define:process.env.NODE_ENV=\\\"production\\\" --analyze"
  },
  "devDependencies": {
    "@types/prismjs": "^1.26.0",
    "stats.js": "^0.17.0",
    "tsconfig": "*",
    "config": "*"
  }
}
